export const GRID = {
  UNIT_SIZE: 1, // Base unit size (1 = one grid square)
  VISUAL_MULTIPLIER: 28.5, // Pixels per grid unit (adjusted from 27.3)
  SECTION_SIZE: 5, // Number of units per grid section
  MIN_MESSAGE_SIZE: 5, // Minimum message size in grid units
  MAX_MESSAGE_SIZE: 100, // Maximum message size in grid units
  CAMERA_HEIGHT: {
    MIN: 20,
    MAX: 100
  }
}
